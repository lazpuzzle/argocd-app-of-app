apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app4
  labels:
    app: nginx-app4
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-app4
  template:
    metadata:
      labels:
        app: nginx-app4
    spec:
      containers:
      - name: nginx
        image: nginx
        resources:
          requests:
            cpu: "100m"
            memory: "10Mi"
          limits:
            cpu: "100m"
            memory: "10Mi"
